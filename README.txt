Install the requirements.txt file in the command window
pip install -r requirements.txt

Run uvicorn app:app --host 0.0.0.0 --port 8000
run python3 main.py to see the implementation of OpenCV


